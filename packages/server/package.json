{
  "name": "@checkspec/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CheckSpec HTTP API server — exposes @checkspec/core over REST",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "node --watch dist/index.js",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@checkspec/core": "^0.2.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
