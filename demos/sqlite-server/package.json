{
  "name": "@checkspec-demos/sqlite-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "CheckSpec demo: SQLite server (sql.js / WebAssembly) with raw query execution — security finding expected",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "sql.js": "^1.12.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/sql.js": "^1.4.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
