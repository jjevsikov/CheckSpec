{
  "name": "@checkspec-demos/filesystem-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "CheckSpec demo: filesystem server with unsanitized paths — security finding expected",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
